import {Layout} from "antd";

const { Footer } = Layout;
const FooterComponent = (props) => {
    return (
        <Footer style={{ textAlign: 'center' }}>Bản quyền bởi lớp WebTC64 - MindX</Footer>
    )
}

export default FooterComponent;